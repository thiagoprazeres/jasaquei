import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.jasaquei.app',
  appName: 'jasaquei',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
